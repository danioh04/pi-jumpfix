# pi-jumpfix

A lightweight Pi extension to prevent the TUI from jumping to the top of the conversation and wiping your scrollback while the model streams.

## Installation

```bash
pi install npm:pi-jumpfix
```

Or from GitHub:

```bash
pi install git:github.com/danioh04/pi-jumpfix
```
